apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: exam-board
  annotations:
    argocd.argoproj.io/sync-wave: "4"

spec:
  type: ClusterIP

  selector:
    app: admin-service

  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP